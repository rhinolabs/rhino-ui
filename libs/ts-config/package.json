{
  "name": "@rhino-ui/ts-config",
  "author": "@rhinolabs",
  "version": "1.0.0",
  "private": true,
  "description": "Typescript configuration for Rhino UI",
  "main": "tsconfig.base.json",
  "scripts": {
    "eslint": "eslint tsconfig.*.json --fix",
    "format": "prettier --write tsconfig.*.json --cache "
  },
  "dependencies": {}
}
