import { Meta } from '@storybook/addon-docs';

<Meta title="Components/Pagination/Codesandbox" />

# Pagination

Use pagination to improve user scanning, as well as load times, for large collections or lists.

```js codesandbox=rhinolabs-components
import React, { useState } from 'react';
import { Pagination } from '@rhino-ui/components';

export default () => {
  const [activePage, setActivePage] = useState(1);

  return (
    <Pagination
      activePage={activePage}
      itemsPerPage={20}
      onChange={setActivePage}
      totalItemsCount={1000}
      isCompact
      arePagesVisible
      numberOfPagesDisplayed={3}
    />
  );
};
```
